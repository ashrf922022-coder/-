# وثيقة المعمارية الأمنية وحماية البيانات — AWRIDI AI Gold

تحدد هذه الوثيقة المعمارية الأمنية الشاملة لتطبيق **AWRIDI AI Gold (XAU/USD)**، بما في ذلك الوضع الأمني الحالي، تقييم المخاطر، معايير التشفير، التصميم الهندسي المستقبلي للربط مع السيرفر والخادم الخلفي (Backend)، وحماية هندسة التطبيق (APK) والاشتراكات.

---

## 1. الوضع الأمني الحالي (Current Security Posture)

1. **لا توجد مفاتيح أو أسرار مضمنة (Zero Hardcoded Secrets)**:
   - لا يحتوي الكود المصدري أو ملفات `build.gradle` أو ملفات JSON في `assets` على أي أسرار، مفاتيح API لـ Twelve Data، أو Bot Tokens لـ Telegram.
   - يتم إدخال مفاتيح API ورموز البوت بشكل اختياري من قبل المستخدم عبر شاشة الإعدادات.

2. **التخزين المحلي الآمن**:
   - يتم تخزين إعدادات المستخدم محليًا في `SharedPreferences` مع دعم التشفير باستخدام **Android Keystore** عبر `EncryptedPrefsHelper`.

3. **منع النسخ الاحتياطي غير المشفّر**:
   - تم تعطيل النسخ الاحتياطي التلقائي عبر ADB في `AndroidManifest.xml` (`android:allowBackup="false"` و `android:fullBackupContent="false"`) لمنع التسريب عند استخراج نسخ الشاشة.

---

## 2. المخاطر المكتشفة وتقييم الأثر (Discovered Risks)

| نوع الخطر | مستوى الخطر | الوصف والتقييم | المعالجة المنفذة / المقترحة |
| :--- | :---: | :--- | :--- |
| **تسريب مفتاح API على الهاتف** | متوسط | وجود API Key محليًا على هاتف الكلاينت يجعل المفتاح عرضة للهندسة العكسية إذا تمت مهارة الروت. | التشفير بـ Android Keystore AES-GCM + النقل المستقبلي لـ Backend. |
| **تجاوز هندسة APK العكسية** | متوسط | تفكيك ملف APK وقراءة منطق الاستراتيجية ومحرك القرار. | تفعيل R8 / ProGuard وقواعد Repackaging و Obfuscation. |
| **استخراج النسخ الاحتياطية (ADB Backup Leak)** | منخفض | استخراج بيانات SharedPreferences عبر بروتوكول ADB بدون روت. | تم التعطيل بنجاح في `AndroidManifest.xml`. |
| **مخاطر حزم البيانات الحية (MitM Attacks)** | متوسط | اعتراض حزم شبكة SSL/TLS أو هجمات السيرفر الوسيط. | فرض اتصالات HTTPS فقط ودعم SSL Pinning مستقبليًا. |

---

## 3. جرد البيانات الحساسة (Sensitive Data Inventory)

- **مفتاح API الخاص بـ Twelve Data**: مستند إلى إدخال المستخدم وتخزينه محليًا.
- **Telegram Bot Token & Chat ID**: مستند إلى إدخال المستخدم محليًا لإرسال التنبيهات.
- **رأس المال التجريبي ونسبة المخاطرة**: إعدادات حسابية تجريبية محليًا.
- **سجل الصفقات التجريبية**: بيانات مخزنة بترميز JSON محليًا.

---

## 4. حدود البيانات: الهاتف مقابل الخادم الخلفي (Mobile vs. Backend Boundaries)

```
[ AWRIDI Mobile App ] ──(HTTPS Client Request)──> [ AWRIDI Secured Backend ]
                                                         │
                                               (Central Master API Keys)
                                                         ▼
                                             [ Twelve Data / Market API ]
```

### ما يبقى على الهاتف (Mobile Device):
- واجهة المستخدم والتفاعل RTL Arabic UI.
- التفضيلات المحلية ورأس المال التجريبي.
- محرك الحسابات السريعة المؤقت وإرشادات المساعد التعليمي.

### ما يجب نقله مستقبلاً إلى الخادم الخلفي (AWRIDI Backend):
- **مفاتيح Twelve Data API**: تخزين مفاتيح المزود الرئيسية في السيرفر الخلفي المكتوم فقط.
- **خوارزميات القرار المتقدمة**: تشغيل استراتيجية الذهب وحساب الإشارات في السيرفر وتغذية الهاتف بالنتائج فقط.
- **Telegram Webhook Broadcasting**: إدارة تنبيهات التليجرام مركزياً من السيرفر.

---

## 5. التصميم الموصى به مستقبلاً (Target Security Architecture)

```
+------------------------+        HTTPS / TLS        +--------------------------+
|  AWRIDI Android App    |  ======================>  |  AWRIDI Cloud Backend    |
|  (No API Keys Stored)  |   App Token / Device ID   |  (Node.js / Go / Python) |
+------------------------+                           +--------------------------+
                                                                  │
                                                        Master Twelve Data API Key
                                                                  ▼
                                                     +--------------------------+
                                                     | Twelve Data Market API   |
                                                     +--------------------------+
```

---

## 6. نظام المصادقة والاشتراكات والترخيص (Authentication & Subscriptions)

1. **مصادقة المستخدم برقم الهاتف / البريد**:
   - استخدام OAuth 2.0 / JWT (JSON Web Tokens) للحصول على Access Token مع انقضاء صلاحية محدد (Short-Lived Tokens).
2. **الترخيص والاشتراكات (License Validation)**:
   - التحقق من حالة اشتراك المستخدم عبر السيرفر في كل عملية سحب بيانات للذهب.

---

## 7. ربط الجهاز ومنع الإساءة (Device Binding & Anti-Abuse)

- **Device Fingerprinting / Hardware Binding**:
  - إنشاء معرف مشفر فريد للجهاز (`Android UUID` + KeyStore Hash) لمنع مشاركة الحساب الواحد بين عدة أجهزة في نفس الوقت.
- **Rate Limiting**:
  - تقييد عدد طلبات التحليل لكل دقيقة لمنع السكربتات المجهولة من استنزاف الخادم.

---

## 8. استراتيجية حماية مفاتيح API (API Key Protection)

- **المنع التام للتخزين المباشر في APK**:
  - عدم وضع أي مفتاح API كرمز ثابت داخل الكود المصدر Java أو ملفات Gradle أو Native Libraries.
- **Dynamic Proxy Architecture**:
  - لا يتصل الهاتف بـ Twelve Data مباشرة، بل يطلب السيرفر الخاص بـ AWRIDI الذي يضيف المفتاح من طرفه بأمان ويتحقق من صلاحية المستخدم.

---

## 9. حماية APK وتعمية الكود (R8 / ProGuard Obfuscation)

تم إعداد ملف `app/proguard-rules.pro` وتفعيله في `app/build.gradle`:
- **Minification & Shrinking**: حذف الكود غير المستعمل وتقليل حجم الملف.
- **Name Obfuscation**: تغيير أسماء المتغيرات والدوال إلى رموز غير مفهومة (`a`, `b`, `c`).
- **Repackaging**: إعادة تجميع الكود المصدري لمنع قراءة البنية الهيكلية.

---

## 10. التشفير والتخزين عبر Android Keystore

يوفر كلاس `EncryptedPrefsHelper.java` حماية من المستوى الأول:
- إنشاء مفتاح AES-GCM 256-bit داخل شريحة **Android KeyStore** الآمنة في الجهاز.
- تشفير القيم الحساسة المكتوبة في الإعدادات بأسلوب معتمد غير قابل لفك التشفير خارجيًا.

---

## 11. مراحل التنفيذ المستقبلية (Implementation Roadmap)

| المرحلة | الأهداف والأنشطة |
| :---: | :--- |
| **المرحلة 1 (الحالية)** | تشفير البيانات بـ Android Keystore ، تفعيل R8/ProGuard ، منع ADB Backup. |
| **المرحلة 2** | إنشاء خادم AWRIDI Backend وتوجيه طلبات Twelve Data من خلاله. |
| **المرحلة 3** | تفعيل تسجيل الدخول واشتراكات المستخدمين وتوثيق JWT مع SSL Pinning. |

---

## 12. سلامة التداول وأمن المستهلك

- **تطوير تعليمي وتحليلي فقط**: لا يتم تنفيذ أي أمر مالي حقيقي أو ربط مع حسابات بروكر حقيقية.
- **إخلاء مسؤولية واضحة**: يحتوي النظام على تحذير صريح باللغة العربية يؤكد أن نتائج التحليل والاختبارات ليست ضمانًا للربح ولا تمنع الخسارة.
